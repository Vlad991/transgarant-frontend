import React from "react";
import DriverDataForm from "./DriverDataForm";

const DriverData = ({title, state}) => {
    return (
        <DriverDataForm title={title}/>
    );
}

export default DriverData;